# Raw source data stays out of version control; BUILD-STAMP.tsv + PROVENANCE.tsv
# record what was there and build-data.R rebuilds derived/ from it.
**/data/raw/
*.zip

# Tooling litter
node_modules/
__pycache__/
*.pyc
.Rhistory
.RData
.Rproj.user/
rerender.log

# macOS
.DS_Store

# A full clone of this repo landed here on 1 Sep 2026. Nothing references it
# and it duplicates 429 MB into Drive sync; ignored so no `git add -A` can
# commit it into the public repo.
/democracys-data/
